# localSpace 设计方案 stage2

## 整体需求

### 导入过程Agent生成标签和描述逻辑优化
- 支持为AI提供一些关键词，这样生成出来的标签和描述更精准
- 支持读取用户输入的标签作为提示词的一部分
- 支持从网络中进行搜索获取关键信息（是否可以通过agent的tool来让Agent决定如何搜索）
- 对以上信息整合后在通过大模型进行 标签和描述的生成


### 批量导入设计
- 支持选择多个文件然后批量导入
这里有个问题，我应该如何才能和之前的单个文件导入不冲突呢



### 展示中的元数据编辑



### 扫描指定目录下文件，发现文件导入到软件中





